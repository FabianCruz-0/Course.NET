using System;
using System.Collections;
using System.Collections.Generic;

namespace fundamentosC
{
    class Program
    {
        static void Main(string[] args)
        {
            int[] intarray = new int[4];
            System.Console.WriteLine("Hola mundo");

            /*
            ArrayList es un tipo de dato de arreglos en los cuales se pueden poner elementos de
            distintos tipos de datos.

            Para hacer una variable de este tipo es:
            ArrayList myArray = new ArrayList();

            Para agregar elementos a un ArrayList se usa: nombeArray.add(Valor a introducir);
            Para imprimrlo se intriduce el indice del dato a mostrar (empieza desde 0).

            También hay otros métodos como:
            .Remove() Remueve el dato exacto.
            .RemoveAt() Remuve el dato según su índice.


            Para usar rangos se necesitan otra variable de colección ompatible con la variable.
            .AddRange() Agrega un rango.
            .InsertRange() Inserta un rango a partir de su indice.


    
            LISTAS

            Ejemplo:
            List<int> mylist = new List<int>();

            Métodos:
            mylist.Add();
            .Count
            .Insert




            STRINGS

            string mistring = null;
            string mistring2 = "";
            string mistring3 = "Hola mundo";
            const string mistring4 = "Este texto no se puede modificar";


            **PARA LOS EJERCICIOS RVISAR EL ARCHIVO COMPRIMIDO**


            CLASES
            
             */
        }
    }
}